services:
  backend:
    build: .
    env_file: 
      - .env
    ports: 
      - "3000:3000"
  db:
    image: postgres:latest
    env_file: 
      - .env
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data: